/**
 *
      // Benefit for using a type.d.ts file is that You dont need to import it.
      // They are available globally.
 *
 * @interface User
 */
interface User {
  name: string;
  email: string;
  image: string;
  id: string;
}
